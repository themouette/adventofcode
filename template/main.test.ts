import { executePart1, executePart2 } from "./main";

const exampleInput = ``;

describe("part 1", () => {
  it("example", () => {
    expect(executePart1(exampleInput)).toBe(1);
  });
});
describe("part 2", () => {
  it("example", () => {
    expect(executePart2(exampleInput)).toBe(1);
  });
});
